diff -ruN a/packages/opencode/src/tool/skill.ts b/packages/opencode/src/tool/skill.ts
--- a/packages/opencode/src/tool/skill.ts
+++ b/packages/opencode/src/tool/skill.ts
@@ -19,7 +19,7 @@
     const rg = yield* Ripgrep.Service
     return () =>
       Effect.gen(function* () {
-        const list = yield* skill.available().pipe(Effect.provide(EffectLogger.layer))
+        const list = yield* skill.available().pipe(Effect.provide(EffectLogger.layer), Effect.map((x) => [...x].sort((a, b) => a.name.localeCompare(b.name))))

         const description =
           list.length === 0
